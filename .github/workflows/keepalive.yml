name: keep-supabase-alive
on:
  schedule:
    - cron: "0 6 * * *"   # tiap hari 06:00 UTC (~13:00 WIB)
  workflow_dispatch:       # bisa jalan manual dari tab Actions
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl -sS --fail "https://syotgqexlrornrkaibnf.supabase.co/functions/v1/tebar-api/ping"
